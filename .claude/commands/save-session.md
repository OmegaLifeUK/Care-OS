Save the full conversation history of this current session to a markdown file.

Instructions:
1. Read the `sessions/` folder in the project root to find the latest session number
2. Create a new file called `sessions/session{N+1}.md` (incrementing from the last session number)
3. Write the COMPLETE conversation from the very start of this session — every user message, every Claude response (summarized with key details), every action taken, every command run, every file created/modified
4. Include the date, branch, and working directory at the top
5. Include a "Session Status at End" section with what's done and what's next
6. Log this action in `logs.md`
7. Confirm to the user with the filename created
